* Providers Overview
ai-glib provides a unified interface to multiple AI providers through the =AiProvider= interface. All provider clients extend =AiClient= and implement =AiProvider= and =AiStreamable= interfaces.

** Supported Providers
*** HTTP API Providers
| Provider                   | Client Class     | Environment Variable                    | Default Model            |
|----------------------------+------------------+-----------------------------------------+--------------------------|
| [[file:claude.org][Claude]] | =AiClaudeClient= | =ANTHROPIC_API_KEY= or =CLAUDE_API_KEY= | claude-sonnet-5          |
| [[file:openai.org][OpenAI]] | =AiOpenAIClient= | =OPENAI_API_KEY=                        | gpt-5.5                  |
| [[file:gemini.org][Gemini]] | =AiGeminiClient= | =GEMINI_API_KEY=                        | gemini-3.5-flash         |
| [[file:grok.org][Grok]]     | =AiGrokClient=   | =XAI_API_KEY= or =GROK_API_KEY=         | grok-4.3                 |
| [[file:ollama.org][Ollama]] | =AiOllamaClient= | =OLLAMA_HOST= (optional)                | gpt-oss:20b              |

*** CLI Wrapper Providers
| Provider                                         | Client Class         | Environment Variable          | Default Model                      |
|--------------------------------------------------+----------------------+-------------------------------+------------------------------------|
| [[file:claude-code.org][Claude Code]]             | =AiClaudeCodeClient= | =CLAUDE_CODE_PATH= (optional) | sonnet                             |
| [[file:claude-code-tmux.org][Claude Code (tmux)]] | =AiClaudeTmuxClient= | =CLAUDE_CODE_PATH= (optional) | sonnet                             |
| [[file:opencode.org][OpenCode]]                   | =AiOpenCodeClient=   | =OPENCODE_PATH= (optional)    | anthropic/claude-sonnet-5          |
| [[file:grok-build.org][Grok Build]]              | =AiGrokBuildClient=  | =GROK_PATH= (optional)        | grok-4.6                           |
| [[file:antigravity.org][Antigravity]]            | =AiAntigravityClient= | =AGY_PATH= (optional)        | gemini-3.7-flash-high              |
| [[file:cursor.org][Cursor]]                  | =AiCursorClient=      | =CURSOR_AGENT_PATH= / =CURSOR_PATH= | auto                          |

The two Claude Code variants drive the same =claude= CLI but differ in billing path: =AiClaudeCodeClient= uses =claude --print= (Agent SDK credits), while =AiClaudeTmuxClient= runs the interactive TUI inside an ephemeral tmux session and consumes the user's normal subscription budget. See [[file:claude-code-tmux.org]] for details.

** Common Interface
All providers implement the =AiProvider= interface:

#+begin_src C
/* Get provider information */
AiProviderType ai_provider_get_provider_type(AiProvider *provider);
const gchar *ai_provider_get_name(AiProvider *provider);
const gchar *ai_provider_get_default_model(AiProvider *provider);

/* Chat completion */
void ai_provider_chat_async(AiProvider *provider,
                            GList *messages,
                            const gchar *system_prompt,
                            gint max_tokens,
                            GList *tools,
                            GCancellable *cancellable,
                            GAsyncReadyCallback callback,
                            gpointer user_data);

AiResponse *ai_provider_chat_finish(AiProvider *provider,
                                    GAsyncResult *result,
                                    GError **error);
#+end_src

** Creating a Client
Each provider has multiple constructors:

#+begin_src C
/* Using environment variables (recommended) */
g_autoptr(AiClaudeClient) client = ai_claude_client_new();

/* Using explicit configuration */
g_autoptr(AiConfig) config = ai_config_new();
ai_config_set_api_key(config, AI_PROVIDER_CLAUDE, "sk-...");
g_autoptr(AiClaudeClient) client = ai_claude_client_new_with_config(config);

/* Using API key directly */
g_autoptr(AiClaudeClient) client = ai_claude_client_new_with_key("sk-...");
#+end_src

** Setting the Model
After creating a client, you can change the model:

#+begin_src C
g_autoptr(AiClaudeClient) client = ai_claude_client_new();

/* Set a different model */
ai_client_set_model(AI_CLIENT(client), AI_CLAUDE_MODEL_OPUS_5);

/* Or use the string directly */
ai_client_set_model(AI_CLIENT(client), "claude-haiku-4-5-20251001");
#+end_src

** Provider Comparison
| Feature          | Claude | OpenAI | Gemini | Grok | Ollama                | Claude Code        | Claude Code (tmux) | OpenCode           | Grok Build         | Antigravity        | Cursor             |
|------------------+--------+--------+--------+------+-----------------------+--------------------+--------------------+--------------------+--------------------+--------------------+--------------------|
| Chat Completion  | Yes    | Yes    | Yes    | Yes  | Yes                   | Yes                | Yes                | Yes                | Yes                | Yes                | Yes                |
| Streaming        | Yes    | Yes    | Yes    | Yes  | Yes                   | Yes                | No                 | Yes                | Yes                | Yes                | Yes                |
| Tool Use         | Yes    | Yes    | Yes    | Yes  | Yes (model-dependent) | Yes                | Yes                | Model-dependent    | Yes (CLI built-in) | Yes (CLI built-in) | Yes (CLI built-in) |
| Vision           | Yes    | Yes    | Yes    | Yes  | Model-dependent       | Yes                | Yes                | Model-dependent    | No                 | Yes                | Yes                |
| Local            | No     | No     | No     | No   | Yes                   | No                 | No                 | No                 | No                 | No                 | No                 |
| API Key Required | Yes    | Yes    | Yes    | Yes  | No                    | No (uses CLI auth) | No (uses CLI auth) | No (uses CLI auth) | No (uses CLI auth) | No (uses CLI auth) | Optional (=CURSOR_API_KEY=) |
| Multi-Provider   | No     | No     | No     | No   | No                    | No                 | No                 | Yes                | No                 | Yes                | Yes                |
| Billing Path     | API    | API    | API    | API  | n/a                   | Agent SDK          | Subscription (TUI) | Agent SDK          | xAI account        | Google account     | Cursor account     |

** Error Handling
All providers use the same error domain (=AI_ERROR=):

#+begin_src C
g_autoptr(GError) error = NULL;
g_autoptr(AiResponse) response = NULL;

response = ai_provider_chat_finish(AI_PROVIDER(client), result, &error);
if (error != NULL)
{
    switch (error->code)
    {
        case AI_ERROR_INVALID_API_KEY:
            g_printerr("Invalid API key\n");
            break;
        case AI_ERROR_RATE_LIMITED:
            g_printerr("Rate limited, retry later\n");
            break;
        case AI_ERROR_NETWORK_ERROR:
            g_printerr("Network error: %s\n", error->message);
            break;
        default:
            g_printerr("Error: %s\n", error->message);
    }
}
#+end_src
