* AiClient
Base class for all provider clients.

** Hierarchy
#+begin_example
GObject
└── AiClient
    ├── AiClaudeClient
    ├── AiOpenAIClient
    ├── AiGeminiClient
    ├── AiGrokClient
    └── AiOllamaClient
#+end_example

** Description
=AiClient= is the abstract base class for all AI provider clients. It provides common functionality for configuration, model selection, and HTTP communication via libsoup.

Subclasses must implement:

- =build_request()= - Build provider-specific JSON request
- =parse_response()= - Parse provider-specific JSON response
- =get_endpoint_url()= - Return the API endpoint URL
- =add_auth_headers()= - Add authentication headers

** Functions
*** ai_client_get_config
#+begin_src C
AiConfig *
ai_client_get_config(AiClient *self);
#+end_src

Gets the configuration object.

*Parameters:*

- =self=: an AiClient

*Returns:* =(transfer none)=: the AiConfig, do not free

--------------

*** ai_client_get_model
#+begin_src C
const gchar *
ai_client_get_model(AiClient *self);
#+end_src

Gets the current model name.

*Parameters:*

- =self=: an AiClient

*Returns:* =(transfer none) (nullable)=: the model name

--------------

*** ai_client_set_model
#+begin_src C
void
ai_client_set_model(
    AiClient    *self,
    const gchar *model
);
#+end_src

Sets the model to use for requests.

*Parameters:*

- =self=: an AiClient
- =model=: the model name

--------------

*** ai_client_get_max_tokens
#+begin_src C
gint
ai_client_get_max_tokens(AiClient *self);
#+end_src

Gets the default max tokens setting.

*Parameters:*

- =self=: an AiClient

*Returns:* the max tokens value

--------------

*** ai_client_set_max_tokens
#+begin_src C
void
ai_client_set_max_tokens(
    AiClient *self,
    gint      max_tokens
);
#+end_src

Sets the default max tokens for responses.

*Parameters:*

- =self=: an AiClient
- =max_tokens=: maximum tokens to generate

--------------

*** ai_client_get_temperature
#+begin_src C
gdouble
ai_client_get_temperature(AiClient *self);
#+end_src

Gets the sampling temperature.

*Parameters:*

- =self=: an AiClient

*Returns:* the temperature (0.0 to 2.0)

--------------

*** ai_client_set_temperature
#+begin_src C
void
ai_client_set_temperature(
    AiClient *self,
    gdouble   temperature
);
#+end_src

Sets the sampling temperature.

*Parameters:*

- =self=: an AiClient
- =temperature=: temperature value (0.0 to 2.0)

--------------

*** ai_client_get_system_prompt
#+begin_src C
const gchar *
ai_client_get_system_prompt(AiClient *self);
#+end_src

Gets the default system prompt.

*Parameters:*

- =self=: an AiClient

*Returns:* =(transfer none) (nullable)=: the system prompt

--------------

*** ai_client_set_system_prompt
#+begin_src C
void
ai_client_set_system_prompt(
    AiClient    *self,
    const gchar *prompt
);
#+end_src

Sets the default system prompt.

*Parameters:*

- =self=: an AiClient
- =prompt=: =(nullable)=: the system prompt

** Properties
=AiClient= exposes its configuration as GObject properties, so language bindings (Python, GJS, Vala, ...) can use native property syntax and the plain =g_object_set= / =g_object_bind_property= APIs work as expected.

| Property        | Type                  | Default           | Equivalent method                                    |
|-----------------+-----------------------+-------------------+------------------------------------------------------|
| =config=        | =AiConfig=            | created on demand | =ai_client_get_config=                               |
| =model=         | =string= (nullable)   | NULL              | =ai_client_get_model= / =_set_model=                 |
| =max-tokens=    | =int= (1 to G_MAXINT) | 4096              | =ai_client_get_max_tokens= / =_set_max_tokens=       |
| =temperature=   | =double= (0.0 to 2.0) | 1.0               | =ai_client_get_temperature= / =_set_temperature=     |
| =system-prompt= | =string= (nullable)   | NULL              | =ai_client_get_system_prompt= / =_set_system_prompt= |

All properties are read/write. The plain getter/setter functions remain the canonical C API; the properties simply dispatch through them.

From C:

#+begin_src C
g_object_set(client,
             "model", "claude-sonnet-4-5",
             "max-tokens", 1024,
             "temperature", 0.7,
             NULL);
#+end_src

From Python (PyGObject):

#+begin_src python
import gi
gi.require_version("AiGlib", "1.0")
from gi.repository import AiGlib

client = AiGlib.ClaudeClient.new()
client.props.model = "claude-sonnet-4-5"
client.props.max_tokens = 1024
client.props.temperature = 0.7
#+end_src

From JavaScript (GJS):

#+begin_src javascript
const AiGlib = imports.gi.AiGlib;
const client = new AiGlib.ClaudeClient();
client.model = "claude-sonnet-4-5";
client.maxTokens = 1024;
#+end_src

** Example
#+begin_src C
/* Get a client (from any provider) */
g_autoptr(AiClaudeClient) claude = ai_claude_client_new();
AiClient *client = AI_CLIENT(claude);

/* Configure the client */
ai_client_set_model(client, "claude-sonnet-4-20250514");
ai_client_set_max_tokens(client, 4096);
ai_client_set_temperature(client, 0.7);
ai_client_set_system_prompt(client, "You are a helpful assistant.");

/* Check current settings */
g_print("Model: %s\n", ai_client_get_model(client));
g_print("Max tokens: %d\n", ai_client_get_max_tokens(client));
g_print("Temperature: %.1f\n", ai_client_get_temperature(client));
#+end_src

** See Also
- [[file:ai-provider.org][AiProvider]] - Provider interface
- [[file:ai-config.org][AiConfig]] - Configuration
